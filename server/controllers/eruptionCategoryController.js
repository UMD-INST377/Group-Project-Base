const categoryGet = `SELECT category_id, category
FROM eruption_category;`

const categoryPut = `SELECT category_id, category
FROM eruption_category;`

const categoryPost = `SELECT category_id, category
FROM eruption_category;`

const categoryDelete = `SELECT category_id, category
FROM eruption_category;`

export default {
  categoryGet, categoryPut, categoryPost, categoryDelete
};