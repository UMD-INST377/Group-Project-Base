const pricesGet = 'SELECT vinyl_id, highest_discog, lowerst_discog, highest_amazon FROM prices;'

const pricesPut = 'SELECT vinyl_id, highest_discog, lowerst_discog, highest_amazon FROM prices;'

const pricesPost = 'SELECT vinyl_id, highest_discog, lowerst_discog, highest_amazon FROM prices;'

const pricesDelete = 'SELECT vinyl_id, highest_discog, lowerst_discog, highest_amazon FROM prices;'

export default{
    pricesGet, pricesPut, pricesPost, priceDelete
};
