const volcanosGet = `SELECT volcano_id, volcano_name, latitude, longitude, volcano_number
FROM volcanos;`

const volcanosPut = `SELECT volcano_id, volcano_name, latitude, longitude, volcano_number
FROM volcanos;`

const volcanosPost = `SELECT volcano_id, volcano_name, latitude, longitude, volcano_number
FROM volcanos;`

const volcanosDelete = `SELECT volcano_id, volcano_name, latitude, longitude, volcano_number
FROM volcanos;`

export default {
  volcanosGet, volcanosPut, volcanosPost, volcanosDelete
};